import Image from 'next/image';
import styled from 'styled-components';

const Ava = styled(Image)`
  border-radius: 50px;
  height: 50px;
  width: 50px;
  cursor: pointer;
`;

export const headerStyles = { Ava };
